services:
  aurogen:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:8000"
    volumes:
      - ./aurogen/.workspace:/app/aurogen/.workspace
    restart: unless-stopped
